<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.playhard.studentmgr">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->


    <!-- baiduMao -->
    <!-- <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> -->
    <!-- <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> -->
    <!-- <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> -->
    <!-- <uses-permission android:name="android.permission.READ_PHONE_STATE" /> -->
    <!-- <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" /> -->
    <!-- <uses-permission android:name="android.permission.WAKE_LOCK" /> -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:configChanges="orientation|screenSize"
        android:icon="@mipmap/ic_chat"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/netword_security_config"
        android:roundIcon="@mipmap/ic_chat"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:name=".activity.ActivityLogin"
            android:theme="@style/AppTheme2"
            android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ActivityMain"
            android:theme="@style/AppTheme2" />
        <activity android:name=".activity.ActivityAddFriend" />

        <!-- <activity -->
        <!-- android:name=".FifthHomeWork.adActivity" -->
        <!-- android:configChanges="orientation|screenSize" -->
        <!-- android:theme="@style/Theme.AppCompat.NoActionBar" /> -->
        <activity android:name=".activity.lifecycle.FirstActivity" />
        <activity android:name=".activity.lifecycle.SecondActivity" />
        <activity
            android:name=".activity.lifecycle.ThirdActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".activity.lifecycle.FourthActivity" />
        <activity android:name=".activity.ActivityPhonePlace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ActivityConfig" />
        <activity android:name=".activity.FriendInfoActivity" />
        <!-- android:theme="@style/AppTheme2" /> -->
        <activity android:name=".activity.RegistActivity" />

        <provider
            android:name=".custom.StudentContentProvider"
            android:authorities="com.playhard.studentmgr.provider"
            android:enabled="true"
            android:exported="true"></provider>
    </application>

</manifest>